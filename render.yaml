services:
  - type: web
    name: career-radar
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 app.py
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
